apiVersion: v1
kind: Service
metadata:
  name: nest-iscsi-gateway
  namespace: nest
  labels:
    app: nest-iscsi-gateway
    app.kubernetes.io/name: nest-iscsi-gateway
    app.kubernetes.io/part-of: nest
spec:
  type: ClusterIP
  selector:
    app: nest-iscsi-gateway
  ports:
  - name: http
    port: 8083
    targetPort: http
    protocol: TCP
