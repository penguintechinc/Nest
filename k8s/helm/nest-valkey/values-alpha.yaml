replicaCount: 1

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi

storage:
  enabled: true
  size: 512Mi
  storageClass: nest-rbd

config:
  maxmemory: "410mb"
  maxmemoryPolicy: allkeys-lru

# Alpha: minimal configuration
healthCheck:
  enabled: true
  livenessProbe:
    enabled: true
    initialDelaySeconds: 10
    periodSeconds: 10
  readinessProbe:
    enabled: true
    initialDelaySeconds: 5
    periodSeconds: 5
