{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "nest-valkey.serviceAccountName" . }}
  labels:
    {{- include "nest-valkey.labels" . | nindent 4 }}
{{- end }}
