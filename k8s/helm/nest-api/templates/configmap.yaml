apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "nest-api.fullname" . }}-config
  labels:
    {{- include "nest-api.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
