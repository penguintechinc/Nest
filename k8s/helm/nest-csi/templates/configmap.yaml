apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "nest-csi.fullname" . }}-config
  labels:
    {{- include "nest-csi.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{- if ne $key "NODE_NAME" }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
  {{- end }}
