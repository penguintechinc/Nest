apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "nest-controller.fullname" . }}-config
  labels:
    {{- include "nest-controller.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
