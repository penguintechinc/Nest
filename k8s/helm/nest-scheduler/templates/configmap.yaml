apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "nest-scheduler.fullname" . }}-config
  labels:
    {{- include "nest-scheduler.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
