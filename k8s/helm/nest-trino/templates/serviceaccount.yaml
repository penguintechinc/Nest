{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "nest-trino.serviceAccountName" . }}
  labels:
    {{- include "nest-trino.labels" . | nindent 4 }}
{{- end }}
