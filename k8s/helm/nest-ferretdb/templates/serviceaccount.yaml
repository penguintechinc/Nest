{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "nest-ferretdb.serviceAccountName" . }}
  labels:
    {{- include "nest-ferretdb.labels" . | nindent 4 }}
{{- end }}
