apiVersion: v1
kind: Service
metadata:
  name: {{ include "nest-ferretdb.fullname" . }}
  labels:
    {{- include "nest-ferretdb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: mongo
      protocol: TCP
      name: mongo
  selector:
    {{- include "nest-ferretdb.selectorLabels" . | nindent 4 }}
