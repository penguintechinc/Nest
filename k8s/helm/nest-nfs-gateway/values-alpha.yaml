image:
  repository: localhost:32000/nest-nfs-gateway
  tag: latest
  pullPolicy: IfNotPresent

replicaCount: 1

resources:
  requests:
    cpu: 100m
    memory: 64Mi
  limits:
    cpu: 250m
    memory: 128Mi

env:
  GIN_MODE: debug
  LOG_LEVEL: debug
