{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "nest-nfs-gateway.serviceAccountName" . }}
  labels:
    {{- include "nest-nfs-gateway.labels" . | nindent 4 }}
{{- end }}
