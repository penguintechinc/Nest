apiVersion: v1
kind: Service
metadata:
  name: {{ include "nest-nfs-gateway.fullname" . }}
  labels:
    {{- include "nest-nfs-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nest-nfs-gateway.selectorLabels" . | nindent 4 }}
