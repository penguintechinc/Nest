apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "nest-nfs-gateway.fullname" . }}-config
  labels:
    {{- include "nest-nfs-gateway.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
