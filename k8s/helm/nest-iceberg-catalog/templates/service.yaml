apiVersion: v1
kind: Service
metadata:
  name: {{ include "nest-iceberg-catalog.fullname" . }}
  labels:
    {{- include "nest-iceberg-catalog.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nest-iceberg-catalog.selectorLabels" . | nindent 4 }}
